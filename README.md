# Cecyl Backend

## Requirements:

- Create a `.env` file with the following content:

```properties
OPENAI_API_KEY=secret-openai-api-key
```

## Run

Run the server with:

```bash
docker compose up -d
```